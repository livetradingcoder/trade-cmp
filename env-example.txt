# Database Configuration
# For local development with MongoDB
MONGODB_URI=mongodb://localhost:27017/trade_arena

# Or use DATABASE_URL (both work, MONGODB_URI takes precedence)
DATABASE_URL=mongodb://localhost:27017/trade_arena

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/trade_arena?retryWrites=true&w=majority

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Encryption Configuration (required for encrypting sensitive data like SMTP passwords)
# Generate a secure random string (32+ characters recommended)
ENCRYPTION_KEY=your-32-character-encryption-key-here-change-in-production

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Email Configuration (Legacy - can be overridden in admin panel)
# SMTP settings can now be configured dynamically through the admin dashboard
# These environment variables serve as fallback if database settings are not configured
# For development, you can leave these empty (emails will be logged to console)
# For production, configure via Admin Dashboard > Settings > Email (SMTP) Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
EMAIL_FROM=LiveTradingLeague <noreply@LiveTradingLeague.com>

# FP Markets ("fpmarkets") Broker Integration
# Account Performance API credentials (beta). Get token/secret from the broker.
# Your server's outbound IP must be whitelisted by the broker first.
FP_MARKETS_BASE_URL=https://ibbeta.fptrading.com
FP_MARKETS_TOKEN=your_fp_access_token
FP_MARKETS_SECRET=your_fp_secret
# Comma-separated rebate/IB account number(s) you own
FP_MARKETS_REBATE_ACCOUNTS=00123,00456

# Frontend Configuration (only needed for local development)
VITE_API_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173
VITE_DISCORD_URL=https://discord.gg/your-server
VITE_LEADERBOARD_URL=https://your-leaderboard-url.com
